import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("@x500/protocol-algorand-v1", () => {
  it("compile script is configured", () => {
    assert.ok(true);
  });
});
